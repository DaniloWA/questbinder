/**
 * SmartSync - Utils Index
 * 
 * Export all utility functions.
 */

export {
  computeDiff,
  applyDiff,
  isDiffEmpty,
  mergeDiffs,
  estimateSize,
  shouldUseDelta,
} from './diff';

export type { EntityDiff } from './diff';
